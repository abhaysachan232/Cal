# Cal
A clean, reusable React + TypeScript Calendar component that renders the correct month based on a given date, highlights the selected day, supports month navigation, and includes full component test coverage using react-testing-library
